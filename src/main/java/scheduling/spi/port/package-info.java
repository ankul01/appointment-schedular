/**
 * Persistence and infrastructure ports (LLD §5, §6.3).
 *
 * <p>Houses: {@code SlotRepository}, {@code AppointmentRepository},
 * plus later outbox / tenant-resolver / cache ports.
 */
package scheduling.spi.port;
